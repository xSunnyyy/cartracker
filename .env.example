# Optional. If set, the /api/quote route uses Finnhub for live US stock
# prices (free tier: 60 req/min, signup at https://finnhub.io/dashboard).
# When unset, the route falls back to Yahoo Finance's unauthenticated chart
# endpoint, which may rate-limit or be blocked in some regions.
FINNHUB_API_KEY=
